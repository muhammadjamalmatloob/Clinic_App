import 'package:screen_retriever/screen_retriever.dart';

void main() async {
  // Can't run without window_manager initialized usually, wait, screen_retriever might need flutter bindings.
  // It's okay, I'll just check the Display properties from its source code.
}
